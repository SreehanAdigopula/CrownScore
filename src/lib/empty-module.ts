/* Browser-bundle stub for Node built-ins referenced by opencv.js's Emscripten glue.
   The glue only touches fs/path when it detects a Node runtime, so an empty module is safe. */
const empty = {};
export default empty;
