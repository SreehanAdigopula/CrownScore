"use client";
/* Demo selection is restored from local storage after hydration. */
/* eslint-disable react-hooks/set-state-in-effect */
import { useEffect,useState } from "react"; import { Bot, ShieldCheck } from "lucide-react"; import { AppShell } from "@/components/layout/AppShell"; import { createDemoDashboard } from "@/server/demo/demo-data"; import { getDemoScenario } from "@/lib/demo-client";
export default function CoachPage(){ const [data,setData]=useState(()=>createDemoDashboard("healthy")); useEffect(()=>setData(createDemoDashboard(getDemoScenario())),[]); return <AppShell title="Coach"><div className="mx-auto max-w-3xl p-4 lg:p-8"><section className="glass-panel rounded-3xl p-6 sm:p-8"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-emerald-400/10 text-emerald-300"><Bot className="size-5"/></span><div><h1 className="font-medium">Folliq coach</h1><p className="text-xs text-zinc-500">Supportive style, mock-safe provider</p></div></div><h2 className="mt-8 text-3xl font-semibold tracking-tight">{data.coach.headline}</h2><p className="mt-4 text-base leading-7 text-zinc-400">{data.coach.summary}</p><div className="mt-7 rounded-2xl bg-white/[.04] p-5"><p className="text-xs font-medium text-emerald-300">Next step</p><p className="mt-2 text-sm leading-6">{data.coach.nextStep}</p></div>{data.coach.regimenObservation&&<p className="mt-5 text-sm text-zinc-500">{data.coach.regimenObservation}</p>}<div className="mt-8 flex gap-3 border-t border-white/8 pt-6"><ShieldCheck className="mt-0.5 size-4 shrink-0 text-emerald-300"/><p className="text-xs leading-5 text-zinc-600">The coach receives structured facts only. It cannot change scores, safety results, database writes, or recommend medication changes.</p></div></section></div></AppShell>; }
