"use client";
import { useRouter } from "next/navigation"; import { useState } from "react"; import { AppShell } from "@/components/layout/AppShell"; import { Button } from "@/components/ui/button"; import { Label } from "@/components/ui/label"; import { Slider } from "@/components/ui/slider"; import { Textarea } from "@/components/ui/textarea";
export default function QuestionnairePage(){ const router=useRouter(); const [adherence,setAdherence]=useState([92]); const [shedding,setShedding]=useState("TYPICAL"); const [irritation,setIrritation]=useState(false); const [pain,setPain]=useState(false); const submit=()=>{ sessionStorage.setItem("folliq-questionnaire",JSON.stringify({adherenceRate:adherence[0],shedding,irritation,scalpPain:pain,routineChanged:false})); router.push("/check-in/analyzing"); }; return <AppShell title="Check-in context"><div className="mx-auto max-w-2xl p-4 lg:p-8"><section className="glass-panel rounded-3xl p-6 sm:p-8"><h1 className="text-2xl font-semibold tracking-tight">Add a little context.</h1><p className="mt-2 text-sm leading-6 text-zinc-500">Optional answers support the safety review. They never override image quality.</p><div className="mt-8 space-y-7"><div><div className="flex items-center justify-between"><Label>Routine adherence</Label><span className="text-sm text-emerald-300">{adherence[0]}%</span></div><Slider value={adherence} onValueChange={setAdherence} max={100} step={1} className="mt-4" /></div><div><Label>Shedding compared with usual</Label><div className="mt-3 grid grid-cols-3 gap-2">{[["LOW","Less"],["TYPICAL","Typical"],["HIGH","More"]].map(([value,label])=><button key={value} onClick={()=>setShedding(value)} className={`rounded-xl border px-3 py-3 text-sm ${shedding===value?"border-emerald-400 bg-emerald-400/10":"border-white/10"}`}>{label}</button>)}</div></div><div className="grid gap-3 sm:grid-cols-2">{[[irritation,setIrritation,"Irritation"],[pain,setPain,"Scalp pain"]].map(([value,setter,label])=><button key={String(label)} onClick={()=> (setter as (v:boolean)=>void)(!value)} className={`rounded-xl border p-4 text-left text-sm ${value?"border-amber-400/50 bg-amber-400/10":"border-white/10"}`}>{String(label)}<span className="float-right text-xs text-zinc-500">{value?"Yes":"No"}</span></button>)}</div><div><Label htmlFor="notes">Notes (optional)</Label><Textarea id="notes" className="mt-2" placeholder="Any major routine changes?" /></div></div><Button className="mt-8 h-12 w-full" onClick={submit}>Analyze check-in</Button></section></div></AppShell>; }
