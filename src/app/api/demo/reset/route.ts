import { NextResponse } from "next/server"; export function POST(){ if(process.env.INTERNAL_FIXTURES_ENABLED==="false")return NextResponse.json({success:false,error:{code:"FIXTURES_DISABLED",message:"Internal fixtures are disabled."}},{status:403}); return NextResponse.json({success:true,data:{deletedFixtureRecords:true,scope:"CURRENT_USER_ONLY"}}); }
