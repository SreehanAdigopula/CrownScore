"use client";
import { useState } from "react"; import { LockKeyhole, Trash2 } from "lucide-react"; import { AppShell } from "@/components/layout/AppShell"; import { Button } from "@/components/ui/button"; import { Dialog,DialogContent,DialogDescription,DialogFooter,DialogHeader,DialogTitle,DialogTrigger } from "@/components/ui/dialog";
export default function SettingsPage(){ const [deleted,setDeleted]=useState(false); return <AppShell title="Settings"><div className="mx-auto max-w-3xl space-y-5 p-4 lg:p-8"><section className="glass-panel rounded-3xl p-6"><h1 className="text-2xl font-semibold">Privacy and data</h1><div className="mt-6 flex gap-3"><LockKeyhole className="mt-0.5 size-5 shrink-0 text-emerald-300"/><div><p className="font-medium">Your progress photos stay private</p><p className="mt-2 text-sm leading-6 text-zinc-500">Images use user-scoped Firebase Storage paths. Location metadata is removed before upload. The demo currently uses local synthetic data.</p></div></div></section><section className="glass-panel rounded-3xl p-6"><h2 className="font-medium">What Folliq can and cannot do</h2><div className="mt-5 space-y-4 text-sm leading-6 text-zinc-400"><p>Scores show relative change from your own baseline. They are not absolute or clinically validated density measurements.</p><p>Expected curves are educational approximations, not medical predictions.</p><p>AI summaries are educational and cannot diagnose, prescribe, or override fixed safety rules.</p></div></section><section className="rounded-3xl border border-red-400/15 bg-red-400/[.04] p-6"><h2 className="font-medium">Delete demo data</h2><p className="mt-2 text-sm text-zinc-500">This clears the selected demo scenario from this browser.</p><Dialog><DialogTrigger asChild><Button variant="destructive" className="mt-5"><Trash2/>Reset all local data</Button></DialogTrigger><DialogContent><DialogHeader><DialogTitle>Reset this browser?</DialogTitle><DialogDescription>This removes local demo preferences and the latest captured preview. It cannot be undone.</DialogDescription></DialogHeader><DialogFooter><Button variant="destructive" onClick={()=>{localStorage.clear();sessionStorage.clear();setDeleted(true);}}>Reset data</Button></DialogFooter></DialogContent></Dialog>{deleted&&<p className="mt-3 text-sm text-emerald-300">Local data cleared.</p>}</section></div></AppShell>; }
