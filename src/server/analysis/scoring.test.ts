import { describe,expect,it } from "vitest"; import { calculateConfidence,classifyTrend,interpolateExpectedCurve,normalizeToBaseline,percentChange } from "@/server/analysis/scoring";
describe("relative scoring",()=>{ it("normalizes a density ratio against the personal baseline",()=>expect(normalizeToBaseline(0.48,0.4)).toBe(120)); it("calculates change between scores",()=>expect(percentChange(110,100)).toBe(10)); it("interpolates between curve points",()=>expect(interpolateExpectedCurve(6,[{week:4,score:98},{week:8,score:102}])).toBe(100)); it("rejects invalid baselines",()=>expect(()=>normalizeToBaseline(0.4,0)).toThrow()); });
describe("quality and trend",()=>{ it("produces bounded confidence",()=>expect(calculateConfidence({brightness:0.58,blur:0.9,contrast:0.45})).toBeGreaterThan(0.8)); it("uses quality before trend",()=>expect(classifyTrend({historyCount:8,confidence:0.3,expectedDeviationPercent:9})).toBe("INSUFFICIENT_QUALITY")); it("flags a meaningful negative deviation",()=>expect(classifyTrend({historyCount:5,confidence:0.9,expectedDeviationPercent:-7})).toBe("WORTH_WATCHING")); });
