import "server-only";
import { getAdminServices } from "@/server/firebase/admin";
import type { CheckInRepository } from "@/server/repositories/contracts";
import type { CheckInAnalysis, CoachOutput } from "@/server/domain/types";
export class FirestoreCheckInRepository implements CheckInRepository { private collection(userId:string){ return getAdminServices().db.collection("users").doc(userId).collection("checkIns"); } async list(userId:string){ const snap=await this.collection(userId).orderBy("capturedAt","asc").get(); return snap.docs.map((doc)=>doc.data().analysis).filter(Boolean); } async get(userId:string,checkInId:string){ const doc=await this.collection(userId).doc(checkInId).collection("analysis").doc("result").get(); return doc.exists?doc.data() as CheckInAnalysis:null; } async saveAnalysis(userId:string,analysis:CheckInAnalysis,coach:CoachOutput){ const ref=this.collection(userId).doc(analysis.id); const batch=getAdminServices().db.batch(); batch.set(ref,{id:analysis.id,userId,capturedAt:analysis.capturedAt,analysisStatus:"COMPLETED",isDemoData:analysis.isDemoData,updatedAt:new Date().toISOString()},{merge:true}); batch.set(ref.collection("analysis").doc("result"),analysis); batch.set(ref.collection("coachSummary").doc("result"),coach); await batch.commit(); } async delete(userId:string,checkInId:string){ await this.collection(userId).doc(checkInId).delete(); } }
